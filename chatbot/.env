DATABASE_URL=postgresql://testuser:testpass@localhost:5432/testdb
# I set my username and password to testuser and testpass, and the database name to testdb
# You can change these values to whatever you want, but make sure to update the database URL in the code accordingly for the insert command to insert the dummy data